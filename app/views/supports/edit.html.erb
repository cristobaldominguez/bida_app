<h1>Editing Support <%= @support.number %></h1>

<%= render 'form', support: @support %>

<%= link_to 'Show', edit_plant_support_path(@plant, @support) %> |
<%= link_to 'Back', plant_supports_path(@plant) %>
