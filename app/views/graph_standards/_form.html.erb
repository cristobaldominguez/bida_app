<%= form_with(model: graph_standard, url: [@plant, graph_standard], local: true) do |form| %>
  <% if graph_standard.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(graph_standard.errors.count, "error") %> prohibited this graph_standard from being saved:</h2>

      <ul>
      <% graph_standard.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :plant_id %>
    <%= form.text_field :plant_id %>
  </div>

  <div class="field">
    <%= form.label :chart_id %>
    <%= form.text_field :chart_id %>
  </div>

  <div class="field">
    <%= form.label :active %>
    <%= form.check_box :active %>
  </div>

  <div class="field">
    <%= form.label :show %>
    <%= form.check_box :show %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
