<% if can? :read, Inspection %>
  <%= render 'shared/buttons/option_button', alt: 'header' do %>
    <li class="option-button__option"><%= link_to t(:show, scope: :global), [@plant, @inspection], class: 'option-button__option-link' %></li>
  <% end %>
<% end %>

<h2 class="main__title">Editing Inspection</h2>

<%= render 'form', inspection: @inspection %>
