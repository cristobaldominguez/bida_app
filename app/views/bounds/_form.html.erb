<%= form_with(model: bound, local: true) do |form| %>
  <% if bound.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bound.errors.count, "error") %> prohibited this bound from being saved:</h2>

      <ul>
      <% bound.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :standard_id %>
    <%= form.text_field :standard_id %>
  </div>

  <div class="field">
    <%= form.label :outlet_id %>
    <%= form.text_field :outlet_id %>
  </div>

  <div class="field">
    <%= form.label :from %>
    <%= form.text_field :from %>
  </div>

  <div class="field">
    <%= form.label :to %>
    <%= form.text_field :to %>
  </div>

  <div class="field">
    <%= form.label :active %>
    <%= form.check_box :active %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
