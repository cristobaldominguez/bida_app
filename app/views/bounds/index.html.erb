<h1>Bounds</h1>

<table>
  <thead>
    <tr>
      <th>Standard</th>
      <th>Outlet</th>
      <th>From</th>
      <th>To</th>
      <th>Active</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @bounds.each do |bound| %>
      <tr>
        <td><%= bound.standard %></td>
        <td><%= bound.outlet %></td>
        <td><%= bound.from %></td>
        <td><%= bound.to %></td>
        <td><%= bound.active %></td>
        <td><%= link_to 'Show', bound %></td>
        <td><%= link_to 'Edit', edit_bound_path(bound) %></td>
        <td><%= link_to 'Destroy', bound, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Bound', new_bound_path %>
