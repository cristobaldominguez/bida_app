<% number = 1 %>
<% if plant.system_size.count > 0 %>
  <% plant.system_size.each do |size| %>
  <%= render 'system_size_size', size: size, number: number %>
  <% number += 1 %>
  <% end %>
<% else %>
<%= render 'system_size_size', size: 0, number: number %>
<% end %>
