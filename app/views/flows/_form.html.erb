<%= form_with(model: flow, local: true) do |form| %>
  <% if flow.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(flow.errors.count, "error") %> prohibited this flow from being saved:</h2>

      <ul>
      <% flow.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :value %>
    <%= form.text_field :value %>
  </div>

  <div class="field">
    <%= form.label :plant_id %>
    <%= form.text_field :plant_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
