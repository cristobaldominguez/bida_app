<h2 class="main__title">Flow Import</h2>

<p>Note: Only import spreadsheets that were previously downloaded from this application.</p>

<h3>Requirements</h3>
<ul>
  <li>Here you could list the validations you have in place for your model</li>
  <li>That way your user has a better chance of successful import</li>
</ul>

<h5>Select File</h5>
<%= form_for @flow_import, url: flows_create_plant_imports_path do |f| %>
  <% if @flow_import.errors.any? %>
    <%= pluralize(@items_import.errors.count, "error") %> prevented us from
    importing your spreadsheet. Please update your spreadsheet and try agin.
    <ul>
      <% @flow_import.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  <% end %>

  <p>
    <%= f.date_select :date, include_blank: true, selected: Date.today %>
  </p>

  <p>
    <%= f.file_field :file %>
  </p>

  <p>
    <%= f.submit "Import File" %>
  </p>
<% end %>
